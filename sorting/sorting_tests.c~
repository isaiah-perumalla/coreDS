
#include "../tinytest.h"
#include "sort.h"

void test_addition()
{
  ASSERT_EQUALS(10, add_numbers(3, 7));
  ASSERT_EQUALS(-10, add_numbers(4, -14));
}


/* test runner */
int main()
{
  RUN(test_addition);

  return TEST_REPORT();
}
